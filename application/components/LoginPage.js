'use strict';
import React, {Component} from 'react';
import {LogForm} from '../layouts/LogForm';

export default class LoginPage extends Component {
    render(){
        return (
            <LogForm/>
        )
    }
}